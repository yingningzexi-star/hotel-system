spring:
  datasource:
    # 默认使用 Windows 身份验证 (integratedSecurity=true)
    url: jdbc:sqlserver://localhost:1433;databaseName=hotel_db;integratedSecurity=true;encrypt=true;trustServerCertificate=true;
    
    # 如果后续需要使用 SQL Login (sa 账户)，请注释掉上面的 url 并启用下面的配置：
    # url: jdbc:sqlserver://localhost:1433;databaseName=hotel_db;encrypt=true;trustServerCertificate=true;
    # username: sa
    # password: your_password
    
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  
  jpa:
    hibernate:
      ddl-auto: update # 自动根据 JPA 实体类同步/更新表结构
    show-sql: true     # 控制台打印 SQL 语句
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.SQLServerDialect
  
  thymeleaf:
    cache: false # 开发阶段关闭 Thymeleaf 模板缓存，修改页面后刷新即可生效
